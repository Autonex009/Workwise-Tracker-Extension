# Base URL of the dashboard backend (FastAPI on Railway).
# Leave unset for local dev — Vite's proxy in vite.config.js forwards /api to
# http://localhost:8200 (or VITE_PROXY_TARGET).
# Set in the hosting provider (Vercel/Netlify) for production builds. No trailing slash.
VITE_API_BASE_URL=
