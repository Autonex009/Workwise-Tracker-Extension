# Local override (gitignored) — points the production extension build at the
# Railway rehearsal backend. Takes precedence over .env.production.
PLASMO_PUBLIC_API_BASE=https://workwise-tracker-extension-production.up.railway.app
